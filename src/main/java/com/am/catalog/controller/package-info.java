/**
 * Пакет controller.
 * Хранит контроллеры Organization, Office, User.
 * Контроллеры принимают параметры, передают их в Сервис и возвращают ResponseEntity.
 * Также хранит ExceptionHandlerController, перехватывающий исключения и возвращающий текст ошибки, завернутый в тэг error.
 */

package com.am.catalog.controller;

